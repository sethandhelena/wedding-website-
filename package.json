{
  "name": "seth-helena-wedding-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply wedding-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply wedding-db --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
